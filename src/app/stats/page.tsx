import { ComingSoon } from "@/components/layout/ComingSoon";

export default function StatsPage() {
  return (
    <ComingSoon
      title="Stats"
      description="Volg je voortgang, winrate en verbetering per niveau — binnenkort beschikbaar."
    />
  );
}
